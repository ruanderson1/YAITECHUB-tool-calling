# Agente LangChain

O terminal recebe uma pergunta, envia ao agente LangChain e imprime a resposta.

Defina `LANGCHAIN_MODEL` e a credencial do provedor escolhido. Depois execute:

```powershell
python app/main.py
```
